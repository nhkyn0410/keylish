# Admin web is a LOCAL-ONLY internal tool (see doc/v2.1.1-admin-local.md).
# Run it against a local API: `pnpm dev:api` (API :3000) + `pnpm dev:admin-web`.
# Not deployed; the admin API surface is closed on the public Render deploy.
NEXT_PUBLIC_API_URL=http://localhost:3000
